apiVersion: v1
kind: Service
metadata:
  name: backend-service # Nombre DNS interno
spec:
  type: ClusterIP # Solo accesible dentro del cluster
  selector:
    app: backend-api
  ports:
    - protocol: TCP
      port: 80 # Puerto expuesto en el Service
      targetPort: 5678 # Puerto del contenedor
